# --- Ядро (нужно с Этапа 0) ---
SQLAlchemy>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
Flask>=3.0
waitress>=3.0   # лёгкий продакшн-WSGI для сервера (без компиляции, ок на 1 ГБ)

# --- Этап 1: сбор из Telegram через публичное веб-превью t.me/s/ (без api_id) ---
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# --- Этапы 5-6 (опц.): отправка в личку / чтение входящих через MTProto (нужен api_id) ---
# Telethon>=1.36

# --- Этапы 2,3,5: парсинг/скоринг/генерация ---
anthropic>=0.40

# --- Этап 1/расписание 24-7 ---
APScheduler>=3.10

# --- Этап 2: дедуп по эмбеддингам (тяжёлый, можно отложить) ---
# sentence-transformers>=3.0

# извлечение текста резюме из PDF (resume_parser); без него письма
# писались по устаревшему тексту из таблицы resumes
pypdf>=6.0
